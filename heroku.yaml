setup:
  addons:
    - plan: heroku-postgresql:hobby-dev
      as: DATABASE

build:
  docker:
    backend:
      dockerfile: backend/Dockerfile
    frontend:
      dockerfile: frontend/src/Dockerfile
release:
  command:
    - echo "Release step for backend"
    - echo "Release step for frontend"
run:
  web: docker-compose up
